# Nginx가 시작되는 데 필요한 최소한의 설정
events {}

http {
    server {
        listen 80;
        server_name localhost;

        # 기본 웹 페이지가 위치할 경로
        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
        }

        # 에러 페이지 설정
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}